import 'package:flutter/material.dart';

class balance extends StatelessWidget {
  

 
   int  initialCoins = 1000;
    final int newCoins;


  balance(this.initialCoins , this.newCoins);


  @override
  Widget build(BuildContext context) {
    return Container();
  }
}